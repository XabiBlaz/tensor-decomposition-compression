[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tn-compression"
version = "0.1.0"
description = "Post-training tensor-decomposition compression for PyTorch models"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
keywords = ["pytorch", "compression", "tensor-decomposition", "cka", "interpretability"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "torch",
  "numpy",
  "scipy",
  "PyYAML",
  "tensorly",
]

[project.optional-dependencies]
vision = ["torchvision", "segmentation-models-pytorch>=0.5,<0.6", "Pillow", "pycocotools"]
deployment = ["onnx", "onnxruntime", "psutil"]
language = ["transformers>=4.51,<4.52", "datasets>=2.18,<3", "sentencepiece"]
demo = [
  "matplotlib",
  "torchvision",
]
test = [
  "pytest",
]

[tool.setuptools.packages.find]
include = ["tn_compression*", "compression*"]

[project.scripts]
tn-compress = "tn_compression.cli:main"
